'use client';

// Clerk으로 교체됨 — SignInButton / useClerk().openSignIn() 사용
// 이 컴포넌트는 하위 호환성을 위해 유지 (실제로 렌더링하지 않음)
export default function LoginModal({ open: _open, onClose: _onClose }: { open: boolean; onClose: () => void }) {
  return null;
}
